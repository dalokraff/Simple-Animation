/*	@adoc shaders
	@node Particles > Billboard Rotation
	@des Returns the rotation of the billboard.
	@req [Output > Particle Base]
*/
group = "Particles"
display_name = "Billboard Rotation"

imports = {
	rotation = {
		type = "float"
		semantic = "TEXCOORD1"
		domain = "vertex"
	}
}

output = {
	name = "Out"
	type = "float"
}

code = """
	RESULT(rotation);
"""
